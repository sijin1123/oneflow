# Inbox Notification Identity Avatars

Runtime evidence for UI-209:

- `sheet-desktop.png`: Topbar notification Sheet with an event-time actor image and actorless system fallback.
- `inbox-mobile.png`: mobile Inbox groups with the same actor image, fallback initials, filters, read action and overflow containment.
