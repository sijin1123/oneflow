# UI-158 Project schedule baseline trend

- `desktop.png`: chronological baseline trend rows, selected-baseline synchronization and variance detail.
- `mobile.png`: responsive trend rows and selected detail without horizontal page overflow.

Both captures are produced by the focused mocked-API Playwright flow. Every trend row is a real button that selects the existing authorized baseline-detail request.
