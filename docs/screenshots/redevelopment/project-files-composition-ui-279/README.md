# UI-279 Project Files Composition

Clean-room runtime captures for the functional Project Files directory.

- `desktop.png`: compact storage facts, upload/link composer, URL-backed discovery and dense file rows at 1440 x 960.
- `mobile.png`: the same functional surface at 390 x 844 with no horizontal overflow.
- `loading.png`: final-geometry skeleton with frame-owned refresh and add actions.
- `error.png`: compact initial error and in-place recovery without replacing the frame.

The captures are generated by the focused Playwright scenario in `apps/web/e2e/smoke.spec.ts`.
