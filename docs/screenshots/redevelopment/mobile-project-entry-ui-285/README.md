# UI-285 Mobile Project Entry Runtime Evidence

These screenshots are OneFlow runtime evidence captured by Playwright at `390x844`.
They are clean-room implementation evidence, not Plane assets or direct Plane mobile
captures.

- `directory-390x844.png`: one-row mobile frame actions, real directory search and
  controls, persisted cover visual, operational rollups, actual current-user role,
  project actions, and viewport-safe Quick Dock.
- `overview-390x844.png`: persisted cover and brief, two-column progress summary,
  project health/member/time signals before schedule content, and viewport-safe
  frame actions and Quick Dock.

The focused E2E also verifies the same flow at `320x740`, real directory-to-Overview
navigation, two-row metric geometry, signal ordering, and zero unintended document
horizontal overflow.
