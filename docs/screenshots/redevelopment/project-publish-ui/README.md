# UI-197 Project Publish Evidence

- `publication-dialog-mobile.png`: owner-only active publication state at `390x844`; the disclosure, public URL, copy/open commands, and two-step revoke entry remain inside the viewport.
- `public-project-desktop.png`: unauthenticated read-only summary at `1440x900`, rendered outside the authenticated application shell.
- `public-project-mobile.png`: the same sanitized summary at `390x844` with single-column progress metrics and no horizontal overflow.

The screenshots are generated by the functional Playwright lifecycle test. API responses are typed fixtures, while publish, copy, route navigation, revoke, and revoked-link handling are exercised through the same UI state transitions as production.
