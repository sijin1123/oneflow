# OneFlow web environment — copy to .env.local for local overrides.
# Build-time only (Vite). NEVER put DB credentials or server secrets here —
# every VITE_* value ships to the browser bundle.

# Backend API base URL (no trailing slash). Restart the dev server after changes.
VITE_ONEFLOW_API_BASE_URL=http://localhost:8000
